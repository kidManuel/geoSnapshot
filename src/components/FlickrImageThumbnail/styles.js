import { createUseStyles } from 'react-jss';

const styles = createUseStyles({
  specialPage: {
    fontSize: 60,
    fontWeight: 900,
    color: 'white',
    lineHeight: '70px',
    width: '100%',
    paddingTop: '5%'
  }
});

export default styles;
